# Node.js
node_modules/
.env
.DS_Store # macOS specific
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build artifacts for common build tools (adjust as needed for your specific setup)
dist/
build/
coverage/

# VS Code specific (if you use it)
.vscode/

# Add any other generated files or sensitive files here
# For example, if Tailwind generates a specific CSS file into a 'public' directory:
# public/output.css